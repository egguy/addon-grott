---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "10 10 * * *"
  workflow_dispatch:

jobs:
  workflows:
    uses: hassio-addons/workflows/.github/workflows/lock.yaml@main
